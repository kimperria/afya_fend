const config = {
  opened: true,
  basename: '',
  defaultPath: '/',
  fontFamily: `'Poppins', san-serif`,
  borderRadius: 12,
  outlinedFilled: true,
  theme: 'light',
  presetColor: 'afya_theme',
  locale: 'en'
};

export default config;
